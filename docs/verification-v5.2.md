# 0.5.2 editing verification

Verified 2026-09-22.

42 automated tests, the production build, and `npm publish --dry-run` pass. Install docs, the agent setup prompt, and hosted update instructions use the v0.5.2 GitHub release tarball. `forma-diagrams` is still not on the npm registry.

0.5.2 lets you resize a selected box, raises a selected line's handles above overlapping lines, and selects a box on the first click. Shift-click and the drag box select only what you aimed at. The sheet footer can be rewritten or hidden.
